{
  "name": "frontier-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "build:production": "pnpm -r build:production && node scripts/collect-dist.ts",
    "build:development": "pnpm -r build:development && node scripts/collect-dist.ts",
    "preview": "pnpm -r --parallel preview",
    "postbuild": "node scripts/collect-dist.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}