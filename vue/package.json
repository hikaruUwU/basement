{
  "name": "base-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @base/web dev",
    "build": "pnpm --filter @base/web build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.7.3"
  }
}
